# Frontend (React + Vite)

This is a minimal React + Vite scaffold that calls the Flask backend APIs.

Install and run:
```bash
cd frontend
npm install
npm run dev
```

You can set an API base URL with the environment variable `VITE_API_BASE`.
